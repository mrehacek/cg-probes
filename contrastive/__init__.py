"""Czech contrastive-pair pipeline for the MU / PU / Topic-Sensitivity probes.

Reads the BERTopic clustering outputs produced by the `search-logs-bertopic/`
stage (this repo) and writes its outputs to `cache/` under this repo root.
"""